// tuples are a group of elements

let tuple:[string,number];
//here the tuple variable must have a array with first element as string
//and second element as number
//order must be the same
tuple = ["name",12]

// the limitation is that pop and push can be done on these tuples